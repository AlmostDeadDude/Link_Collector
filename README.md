# Link Collector

The tool for saving and managing the links. It is possible to add links with tags and categories, search for specific results, view and manage the entire collection. The data is stored in the database, adjust config.php to set it up.
